//
// Created by danis on 12/4/2023.
//
#include <stdio.h>
/*
int main() {
    int x = 5, y = 10;

    // Logical operations
    int logicalOr = x || y;
    int logicalAnd = x && y;
    int logicalNotX = !x;
    int logicalNotY = !y;

    // Bitwise operations
    int bitwiseOr = x | y;
    int bitwiseAnd = x & y;
    int bitwiseNotX = ~x;
    int bitwiseNotY = ~y;

    printf("Logical OR: %d\n", logicalOr);
    printf("Logical AND: %d\n", logicalAnd);
    printf("Logical NOT of x: %d\n", logicalNotX);
    printf("Logical NOT of y: %d\n", logicalNotY);

    printf("Bitwise OR: %d\n", bitwiseOr);
    printf("Bitwise AND: %d\n", bitwiseAnd);
    printf("Bitwise NOT of x: %d\n", bitwiseNotX);
    printf("Bitwise NOT of y: %d\n", bitwiseNotY);

    return 0;
}
*/
